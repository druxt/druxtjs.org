# Vendored: @druxt-contrib/user

The built package, carried here until it is published. Only what the
package publishes is copied: `dist`, the manifest without its scripts and
development dependencies, the licence and the README.

| | |
| --- | --- |
| Package | `@druxt-contrib/user` |
| Commit | `20e7b26` |

Replace `file:vendor/druxt-user` in `package.json` with the published version
once it is on npm, and delete this directory.
