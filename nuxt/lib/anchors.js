// The DOM anchor contract now lives in @druxt-contrib/anchors, shared with ICE
// and the diff view. Re-exported here so the site's wrappers and overlay keep
// importing from '~/lib/anchors'.
module.exports = require('@druxt-contrib/anchors')
